<%= form_for(@quote) do |f| %>
  <% if @quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% @quote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content, "Quotation" %>
    <%= f.text_area :content, rows: 6, class: "span6" %>
  </div>
  <div class="field">
    <%= f.label :source %>
    <%= f.text_field :source, class: "span6" %>
  </div>
  <div class="actions">
    <%= f.submit "Add Quote", class: "btn btn-primary" %>
  </div>
<% end %>
